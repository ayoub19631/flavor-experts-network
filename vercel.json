{
  "framework": "vite",
  "installCommand": "cd app/frontend && corepack enable && pnpm install --frozen-lockfile",
  "buildCommand": "cd app/frontend && corepack enable && pnpm build",
  "outputDirectory": "app/frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
